{
  "author": "OttoHung <totsi04@gmail.com>",
  "bin": {
    "clean": "./dist/clean.js"
  },
  "dependencies": {
    "fs": "latest"
  },
  "description": "Utilities for yarn workspaces",
  "license": "MIT",
  "name": "ts-utils",
  "repository": "git@github.com:OttoHung/typescript.packages.git",
  "scripts": {
    "build": "tsc",
    "clean": "ts-node -r tsconfig-paths/register src/clean.ts",
    "clean:install": "ts-node -r tsconfig-paths/register src/clean.ts --installed",
    "fix": "eslint . --fix --ext .ts",
    "lint": "eslint . --ext .ts",
    "test": "jest"
  },
  "version": "0.1.0"
}
